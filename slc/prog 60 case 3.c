#include <stdio.h>
#include <stdbool.h>

#define MAX_N 100
#define MAX_M 100

int n, m;
char maze[MAX_N][MAX_M];
bool visited[MAX_N][MAX_M];

int main(void) {
    scanf("%d %d", &n, &m);
    // nyerah
    return 0;
}
